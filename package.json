{
  "name": "sandbox-san",
  "version": "1.3.2",
  "private": true,
  "description": "Editor visual de fases 2D SandBox San",
  "scripts": {
    "build": "node scripts/build-standalone.mjs",
    "build:check": "node scripts/build-standalone.mjs --check",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
